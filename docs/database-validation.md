# 第一阶段数据库验证记录

日期：2026-09-21。

使用本机 MySQL 8.0.46 可执行文件创建独立临时实例，绑定 127.0.0.1:13386。未使用用户现有 localhost:3306 实例的账户、凭据或数据。测试完成后临时实例已关闭、临时数据目录已删除，原 MySQL80 服务仍运行。

按顺序执行 `database/01_init.sql`、`database/03_verify.sql`、`database/02_inspect.sql`，均成功。

## 实测结果

| 项目 | 结果 |
| --- | --- |
| 核心表 | 7 张 |
| 主键 | 7 个 |
| 外键 | 11 条 |
| CHECK | 12 条，全部 ENFORCED=YES |
| 独立 UNIQUE 约束 | 3 条；另有 likes 联合主键保证点赞唯一 |
| 验证脚本 | 15 个 PASS 结果 |
| 测试结束各表数据行数 | 全部为 0 |
| 测试结束存储过程数量 | 0 |

通过的断言：大小写用户名唯一、密码哈希 NOT NULL、自加好友拦截、反向重复待处理申请拦截、申请状态与处理时间一致性、历史申请保留并允许重发、重复好友拦截、反向好友拦截、孤立作者外键拦截、空消息拦截、中文与 emoji 保存并在删除好友后保留消息、重复点赞拦截、被引用用户删除限制、删除动态级联清理评论和点赞、全部测试行回滚。

此外，实际外键元数据显示只有 comments.post_id 与 likes.post_id 使用 ON DELETE CASCADE，其余删除规则均为 NO ACTION，符合设计。

## 验证范围

完成 MySQL 真实执行与约束检查，未发现明显建表或关系设计问题。尚未验证后端业务权限、EF Core 映射、API 或 Unity 联调，这些属于后续阶段。未对用户现有数据库执行初始化；用户需按 `database/README.md` 在 Workbench 中执行。
